# Flutter Web runs on a different port than Apache → Image.network is cross-origin.
# These headers allow the browser to load product photos from this folder.
<IfModule mod_headers.c>
  Header set Access-Control-Allow-Origin "*"
  Header set Cross-Origin-Resource-Policy "cross-origin"
</IfModule>
